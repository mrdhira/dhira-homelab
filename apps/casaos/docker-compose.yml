version: "3.9"

services:
  casaos:
    image: casaos/next:latest
    container_name: casaos
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - casaos_data:/casaOS
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  casaos_data:
